"use client";

/**
 * Returns the workspace shell counts that were already resolved on the server.
 */
export function useWorkspaceSignalCounts(initialCounts: {
  notificationCount: number;
  inboxCount: number;
}) {
  return initialCounts;
}
